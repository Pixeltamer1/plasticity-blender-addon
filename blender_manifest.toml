schema_version = "1.0.0"

id = "plasticity_bridge"
version = "2.2.1"
name = "Plasticity"
tagline = "A bridge to Plasticity – live link via WebSocket"
maintainer = "Nick Kallen, User129863"
type = "add-on"

blender_version_min = "4.2.0"

license = ["SPDX:GPL-2.0-or-later"]

[permissions]
network = "Connect to local Plasticity server over WebSocket (localhost:8980 by default)"
